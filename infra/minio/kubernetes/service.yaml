apiVersion: v1
kind: Service
metadata:
  name: minio-service
  labels:
    app: minio
  namespace: filer
spec:
  type: LoadBalancer
  ports:
    - port: 9090
      targetPort: 90
      protocol: TCP
  selector:
    app: minio
