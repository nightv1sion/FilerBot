apiVersion: v1
kind: Service
metadata:
  name: seq
  namespace: filer
  labels:
    app: seq
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
      nodePort: 30003
  selector:
    app: seq
