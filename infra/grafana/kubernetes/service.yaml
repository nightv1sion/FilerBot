apiVersion: v1
kind: Service
metadata:
  name: grafana
  labels:
    app: grafana
  namespace: filer
spec:
  ports:
    - name: web
      port: 3000
      protocol: TCP
      targetPort: 3000
      nodePort: 30003
  sessionAffinity: None
  type: NodePort
  selector:
    app: grafana
